
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");

// load into module namespace from level
require("route");

XT.FileRoute = XT.Route.create({

  init: function() {},
  
  handle: function(info) {
    var resp = info.response;
    XT.throw(XT.E208, info, resp);
  },

  routes: {
    ".html":'',
    ".js":'',
    ".css":'',
  },

  /** @private */
  xtClass: "XT.FileRoute",

});

process.emit("loadedRoute", XT.FileRoute);
