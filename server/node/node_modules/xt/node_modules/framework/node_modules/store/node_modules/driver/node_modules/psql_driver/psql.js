
// load into module namespace from above
require("tools");
require("exception");

// load into module namespace from below
require("psql_query_builder");
require("functor_manager");

XT.PSQLDriver = XT.Object.extend({

  /** @private */
  init: function() {
    this.on("queryBuilt", this.registerQuery);
  },

  //......................................
  // internal methods

  /** @private */
  registerQuery: function(xtQuery) {
    XT.Store.registerQuery(xtQuery);
  },

  //......................................
  // properties

  /** @private */
  queryBuilder: XT.PSQLQueryBuilder,

  /** @private */
  functorManager: XT.FunctorManager,
  
  /** @private */
  xtClass: "XT.PSQLDriver", 

});
