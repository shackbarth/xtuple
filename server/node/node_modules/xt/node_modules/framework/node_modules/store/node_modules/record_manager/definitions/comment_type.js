
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.CommentType = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.CommentType",

    // postgres table
    table: "cmnttype",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "cmnttype_cmnttype_id_seq",

    // fields as columns
    fields: {


      //...................................
      // sproutcore fields

      guid:             "cmnttype_id",

      name:             "cmnttype_name",

      description:      "cmnttype_descrip",

      isSystem:         "cmnttype_sys",

      commentsEditable: "cmnttype_editable",

      order:            "cmnttype_order",
    }

  }, // definition

  /** @private */
  xtClass: "XT.CommentType"

});

XT.Store.recordManager.emit("definitionLoaded", XT.CommentType);
