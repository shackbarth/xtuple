
// generated by __generate_template.py (on 2011-08-12)

// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.IncidentCategory = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.IncidentCategory",

    // postgres table
    table: "incdtcat",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "incdtcat_incdtcat_id_seq",

    // fields as columns
    fields: {


      //...................................
      // sproutcore fields

      guid:                 "incdtcat_id",

      name:                 "incdtcat_name",

      description:          "incdtcat_descrip",

      order:                "incdtcat_order"

    }

  }, // definition

  /** @private */
  xtClass: "XT.IncidentCategory",

});

XT.Store.recordManager.emit("definitionLoaded", XT.IncidentCategory);
