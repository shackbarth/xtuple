
// generated by __generate_template.py (on 2011-08-12)

// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.OpportunitySource = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.OpportunitySource",

    // postgres table
    table: "opsource",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "opsource_opsource_id_seq",

    // fields as columns
    fields: {


      //...................................
      // sproutcore fields

      guid:                 "opsource_id",

      name:                 "opsource_name",

      description:          "opsource_descrip"

    }

  }, // definition

  /** @private */
  xtClass: "XT.OpportunitySource",

});

XT.Store.recordManager.emit("definitionLoaded", XT.OpportunitySource);
