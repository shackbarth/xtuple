
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");

XT.ContactEmail = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.ContactEmail",

    // postgres table
    table: "cntcteml",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "cntcteml_cntcteml_id_seq",

    // fields as columns
    fields: {
      
      //....................................
      // sproutcore fields

      guid:         "cntcteml_id",

      contact:      "cntcteml_cntct_id",

      isPrimary:    "cntcteml_primary",

      email:        "cntcteml_email"
    }

  }, // definition

  /** @private */
  xtClass: "XT.ContactEmail"

});

XT.Store.recordManager.emit("definitionLoaded", XT.ContactEmail);
