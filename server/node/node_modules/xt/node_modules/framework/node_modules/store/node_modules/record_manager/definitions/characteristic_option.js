
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");

XT.CharacteristicOption = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.CharacteristicOption",

    // postgres table
    table: "charopt",

    // postgres schema
    schema: "public",

    // postgres sequence
    sequence: "charopt_charopt_id_seq",

    // fields as columns
    fields: {
      
      //....................................
      // sproutcore fields
      
      guid:           "charopt_id",

      characteristic: "charopt_char_id",

      value:          "charopt_value",

      order:          "charopt_order"
    }

  }, // definition

  xtClass: "XT.CharacteristicOption",

});

XT.Store.recordManager.emit("definitionLoaded", XT.CharacteristicOption);
