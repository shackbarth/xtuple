
// generated by __generate_template.py (on 2011-08-15)

// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.Currency = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.Currency",

    // postgres table
    table: "curr_symbol",

    // postgres schema
    schema: "public",

    // postgres sequenc
    sequence: "curr_symbol_curr_id_seq",

    // fields as columns
    fields: {


      //...................................
      // sproutcore fields

      guid:             "curr_id",

      // FOLLOW SUIT WITH ENTRIES IN THE FORM
      // {SPROUTCORE_FIELD} => {POSTGRES_COLUMN} (without braces or arrow...)
      // FIELDS MUST BE DEFINED EVEN IF IT HAS A TOMANY OR TOONE RELATION
      
      name:             "curr_name",

      
      symbol:           "curr_symbol",

      
      abbreviation:     "curr_abbr",

      
      isBase:           "curr_base",

    }

  }, // definition

  /** @private */
  xtClass: "XT.Currency",

});

XT.Store.recordManager.emit("definitionLoaded", XT.Currency);
