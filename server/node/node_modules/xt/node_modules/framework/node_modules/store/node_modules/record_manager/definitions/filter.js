
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");

XT.Filter = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.Filter",

    // postgres table
    table: "filter",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "filter_filter_id_seq",

    // fields as columns
    fields: {
      
      //....................................
      // sproutcore fields

      guid:         "filter_id",

      view:         "filter_screen",

      name:         "filter_name",

      value:        "filter_value",

      user:         "filter_username",

      selected:     "filter_selected"

    }

  }, // definition

  /** @private */
  xtClass: "XT.Filter",

});

XT.Store.recordManager.emit("definitionLoaded", XT.Filter);
