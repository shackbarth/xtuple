
// generated by __generate_template.py (on 2011-08-12)

// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.OpportunityType = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.OpportunityType",

    // postgres table
    table: "optype",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "optype_optype_id_seq",

    // fields as columns
    fields: {


      //...................................
      // sproutcore fields

      guid:                 "optype_id",

      name:                 "optype_name",

      description:          "optype_descrip"

    }

  }, // definition

  /** @private */
  xtClass: "XT.OpportunityType",

});

XT.Store.recordManager.emit("definitionLoaded", XT.OpportunityType);
