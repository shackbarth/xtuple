
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.Uom = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.Uom",

    // postgres table
    table: "uom",

    // postgres schema
    schema: "public",

    // fields as columns
    fields: {


      //...................................
      // sproutcore fields

      guid:                 "uom_id",

      name:                 "uom_name",

      description:          "uom_descrip",

      isItemWeight:               "uom_item_weight"

    }

  }, // definition

  /** @private */
  xtClass: "XT.Uom"

});

XT.Store.recordManager.emit("definitionLoaded", XT.Uom);
