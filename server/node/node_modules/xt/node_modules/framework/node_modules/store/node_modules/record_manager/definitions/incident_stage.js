
// generated by __generate_template.py (on 2011-08-12)

// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.IncidentStage = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.IncidentStage",

    // postgres table
    table: "status",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "status_status_id_seq",

    // fields as columns
    fields: {


      //...................................
      // sproutcore fields

      guid:                 "status_id",

      code:                 "status_code",

      name:                 "status_name",

      order:                "status_seq",

      color:                "status_color"

    }

  }, // definition

  /** @private */
  xtClass: "XT.IncidentStage",

});

XT.Store.recordManager.emit("definitionLoaded", XT.IncidentStage);
