
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.TaxZone = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.TaxZone",

    // postgres table
    table: "taxzone",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "taxzone_taxzone_id_seq",

    // fields as columns
    fields: {

      //...................................
      // sproutcore fields

      guid:                 "taxzone_id",

      code:                 "taxzone_code",

      description:          "taxzone_descrip"

    }

  }, // definition

  /** @private */
  xtClass: "XT.TaxZone"

});

XT.Store.recordManager.emit("definitionLoaded", XT.TaxZone);
