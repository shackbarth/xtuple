
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");

XT.ProspectCustomer = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.ProspectCustomer",

    // postgres table
    table: "prspctcust",

    // postgres schema
    schema: "xt",

    // fields as columns
    fields: {
      
      //....................................
      // sproutcore fields

      guid:             "prspctcust_id",
    
      number:           "prspctcust_number",

      name:             "prspctcust_name",

      isActive:         "prspctcust_active",

      isCustomer:       "prspctcust_cust"

    }

  }, // definition

  /** @private */
  xtClass: "XT.ProspectCustomer"

});

XT.Store.recordManager.emit("definitionLoaded", XT.ProspectCustomer);
