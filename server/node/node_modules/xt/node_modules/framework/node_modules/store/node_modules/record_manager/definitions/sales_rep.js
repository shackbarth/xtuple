
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");

XT.SalesRep = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.SalesRep",

    // postgres table
    table: "salesrep",

    // postgres schema
    schema: "public",

    // postgres sequence
    sequence: "salesrep_salesrep_id_seq",

    // fields as columns
    fields: {
      
      //....................................
      // sproutcore fields

      guid:             "salesrep_id",
    
      number:           "salesrep_number",

      name:             "salesrep_name",

      isActive:         "salesrep_active",

      commission:       "salesrep_commission",

      employee:         "salesrep_emp_id"

    },
     

  }, // definition

  /** @private */
  xtClass: "XT.SalesRep"

});

XT.Store.recordManager.emit("definitionLoaded", XT.SalesRep);
