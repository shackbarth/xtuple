
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.UomConversion = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.UomConversion",

    // postgres table
    table: "uomconv",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "uomconv_uomconv_id_seq",

    // fields as columns
    fields: {


      //...................................
      // sproutcore fields

      guid:                 "uomconv_id",

      fromUom:              "uomconv_from_uom_id",

      fromValue:            "uomconv_from_value",

      toUom:                "uomconv_to_uom_id",

      toValue:              "uomconv_to_value",

      fractional:           "uomconv_fractional"

    }

  }, // definition

  /** @private */
  xtClass: "XT.UomConversion"

});

XT.Store.recordManager.emit("definitionLoaded", XT.UomConversion);
