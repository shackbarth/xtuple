
// require some core modules
var UTIL    = require("util");
var PATH    = require("path");

// load into module namespace from above
require("tools");
require("exception");
require("store");

// load into module namespace from level
require("record");


XT.ShipVia = XT.Record.extend({

  definition: {

    // sproutcore record equivalent
    sctype: "XT.ShipVia",

    // postgres table
    table: "shipvia",

    // postgres schema
    schema: "public",

    // postgres sequence for guid/id
    sequence: "shipvia_shipvia_id_seq",

    // fields as columns
    fields: {

      //...................................
      // sproutcore fields

      guid:                 "shipvia_id",

      code:                 "shipvia_code",

      description:          "shipvia_descrip"

    }

  }, // definition

  /** @private */
  xtClass: "XT.ShipVia"

});

XT.Store.recordManager.emit("definitionLoaded", XT.ShipVia);
