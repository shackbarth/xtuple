
xt.database.check = function() {
  xt.log("Testing to verify that we can connect to the database...");
  xt.db.query("SELECT * FROM usr;", function(e, r) {
    if(e) throw xt.fatal("Could not connect to database", e);
    xt.log("yes, we can");
  });
}
