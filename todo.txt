- session needs to point to user information and load it if it isn't already
- response needs to handle multiple header types
- cache interface needs to be built (for redis)
- fix warning not being thrown by faulty parsing of json in xt.json
- observers on properties need to be notified by 'set' on xt.objects again
- triggers ('by') need to allow for multiple triggers
- property functions ('.property()') need to be registered observers
- the database abstraction layer needs to be built out and include security
- add cert/key management to require use of SSL/TLS
- add redis configuration options to global settings
- MetaSQL needs to be added back into the stack
- xt npm package needs to be structured as such with dependencies
- should set the correct node path for require
